# PostgreSQL (Drizzle ORM)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/nextjs_template

# Public runtime URL (client-safe)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Useful defaults for container platforms like Coolify
HOSTNAME=0.0.0.0
PORT=3000
